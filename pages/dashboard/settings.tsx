import React from "react";

import { Container, Wrapper } from "@/components";


const Settings = () => {
  return (
    <Wrapper m={{ dt: "Configurações | Code Letters" }}>
      <Container size="sx">
        settings
      </Container>
    </Wrapper>
  );
};

export default Settings;
